#include<iostream>

using namespace std;

void SortArray(int *A, int n){
  sort(A, A+n);

  for(int i=0;i<n;++i)
    cout << A[i] << " ";
  cout << endl;
}

int main(){
  int A[]= {1,3,2,10,8, 12,6};
  int n = sizeof(A)/sizeof(A[0]);

  SortArray(A, n);
			   
  return 0;
}
